# 核心：解密 iPhone 加密备份（内部会装 pycryptodome）
iphone_backup_decrypt==0.9.0

# 不用装别的。新版微信会把极少数超长消息 zstd 压缩后存库，但用的是内嵌在
# 客户端里的私有字典，字典没在备份里，装任何 zstd 库都解不开（详见 README「已知限制」）；
# 工具会把这类消息导成 [未解码的长消息] 占位，不影响其余全部消息正常导出。
